<%= form_for(@notepad) do |f| %>
  <% if @notepad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notepad.errors.count, "error") %> prohibited this notepad from being saved:</h2>

      <ul>
      <% @notepad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field content_data_div">
    <%= f.label :content_data %><br>
    <%= f.text_area :content_data, width: '100000', heigth: '100%' %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
